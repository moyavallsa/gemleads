{
  "name": "gem-leads",
  "version": "1.0.0",
  "description": "Business data scraping solution",
  "scripts": {
    "build": "pip install --upgrade pip && pip install -r requirements.txt",
    "start": "python app.py"
  },
  "engines": {
    "node": "16.x",
    "python": "3.8"
  },
  "dependencies": {
    "@netlify/plugin-python": "^1.0.0",
    "express": "^4.17.1"
  }
} 